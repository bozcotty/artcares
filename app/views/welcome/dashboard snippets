<% else %>
        <% current_user.patient_campaigns.each do |campaign| %>
            <p><%= link_to "Campaign", @patient_campaign_path %>
            <!-- <p><%= link_to "Edit", edit_patient_campaign_path %></p> -->
        <% end %>



<div class= "row">
    <div class ="col-md-4" id="dim_unavailable">
      <% if current_user.patient_campaigns.count >= 1 && current_user.patient_campaigns.artworks.count == 0 %>
        <h5>Your Artworks:</h5>
        <p>(You have no current artworks.)</p>
        <p><%= link_to (image_tag ('/assets/btn-add-artwork.png')), new_patient_campaign_artwork_path %>  
      <% elsif current_user.patient_campaigns.count >= 1 && current_user.patient_campaigns.artworks.count >= 1 %>
        <h5>Your Artworks:</h5>
        <p><%= link_to (image_tag ('/assets/btn-add-artwork.png')), new_patient_campaign_artwork_path %>
      <% end %>
    </div>
  </div>


How i want the artworks to show up when listed:
  <% current_user.patient_campaign.artworks.each do |artwork| %>
    <div class= "row">
      <div class ="col-md-2 col-md-offset-1">
        <%= image_tag(artwork.art_image_1.thumb.url) %>
      </div>
      <div class = "col-md-2">
        <p><%= artwork.title %></p>
        <p>$<%= artwork.price %></p>
      </div>
    </div>
  </br>
  <% end %>

  <% current_user.patient_campaign.artworks.each do |artwork| %>
            <%= link_to artwork.title, patient_campaign_artwork_path(current_user.patient_campaign) %>
  <% end %>